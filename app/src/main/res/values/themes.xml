<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 外部依存ゼロにするため framework の Material テーマを親にする（API 30 で利用可）。 -->
    <style name="Theme.VolNotch" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
