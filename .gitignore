# --- Android / Gradle ---
*.iml
.gradle/
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx

# Gradle Wrapper は生成物だが、生成後は追跡してよい（コメントアウトのまま = 追跡する）
# /gradle/wrapper/gradle-wrapper.jar

# 計画ファイルは追跡しない（ローカルには残す）。
/plan/
