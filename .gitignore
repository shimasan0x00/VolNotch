# --- Android / Gradle ---
*.iml
.gradle/
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx

# Gradle Wrapper は生成物だが、生成後は追跡してよい（コメントアウトのまま = 追跡する）
# /gradle/wrapper/gradle-wrapper.jar

# plan/ は意図的にリポジトリへ含める（無視しない）。
